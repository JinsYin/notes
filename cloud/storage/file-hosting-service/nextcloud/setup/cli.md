# 命令行安装 Nextcloud

```bash
$ occ maintenance:install \
  --admin-user=admin \
  --admin-pass=admin \
  --database=mysql \
  --database-name=nextcloud \
  --database-user=nextcloud \
  --database-pass=password
```