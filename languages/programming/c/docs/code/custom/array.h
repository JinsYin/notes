#include <stdio.h>
#define LEN(arr) (sizeof(arr) / sizeof(arr[0])) // 返回值类型为 size_t

/*
 * 判断数组是否包含某个元素
 */
int contains()
{
    ;
}

/*
 * 返回数组中某个元素的索引
 * 若不存在该元素，返回 -1
 * 若存在多个元素，返回第一个元素的索引
 */
int index()
{
    ;
}