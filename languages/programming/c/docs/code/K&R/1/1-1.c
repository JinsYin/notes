#include <stdio.h>

/*
 * 在你自己的系统中运行 “hello, world” 程序。
 * 再有意去掉程序中中的部分内容，看看会得到什么出错信息
 */
int main()
{
    printf("hello, world\n"); // 打印 “hello, world” 并换行
    printf("hello, world");   // 打印 “hello, world” 不换行

    return 0;
}